import { expect } from 'chai';

describe('Putting this here', () => {
    describe('So the tests pass', () => {
        it('And the build completes', () => {
            expect(true).to.equal(true);
        });
    });
});
